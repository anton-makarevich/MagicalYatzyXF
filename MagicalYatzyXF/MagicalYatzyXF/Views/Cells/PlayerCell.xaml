<?xml version="1.0" encoding="utf-8"?>

<ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Sanet.MagicalYatzy.XF.Controls"
             x:Class="Sanet.MagicalYatzy.XF.Views.Cells.PlayerCell">
    <Grid
        HeightRequest="60">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition />
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>
        <Image 
            Source="{Binding Image, Converter={StaticResource ImageNameConverter}}"
            Style="{StaticResource ProfileImageStyle}">
        </Image>
        <Label 
            Grid.Column="1"
            Text="{Binding Name}"
            VerticalOptions = "Center"
            Style="{StaticResource CaptionLabelStyle}"
            >
        </Label>
        <controls:TappableContentView
            IsVisible="{Binding CanBeDeleted}"
            Grid.Column="2"
            Command="{Binding DeleteCommand}"
            Style = "{StaticResource ToolButtonStyle}"
            >
            <Image 
                Source="{Binding DeleteImage, Converter={StaticResource ImageNameConverter}}"
                Style="{StaticResource ToolButtonImageStyle}"
            >
            </Image>
        </controls:TappableContentView>
    </Grid>
</ViewCell>