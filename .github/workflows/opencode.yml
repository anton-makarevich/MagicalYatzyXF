name: opencode

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

jobs:
  opencode:
    timeout-minutes: 60
    if: |
      (contains(github.event.comment.body, '/oc') ||
      contains(github.event.comment.body, '/opencode')) &&
      github.event.comment.user.type != 'Bot' &&
        contains(
          fromJson('["OWNER"]'),
          github.event.comment.author_association
        )
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Install mise tools
        uses: jdx/mise-action@v4

      - name: Install skills
        run: mise run install-skills

      - name: Install Serena
        run: mise run install-serena

      - name: Assert clean tree
        run: |
          git status --porcelain
          test -z "$(git status --porcelain)" || { echo "::error::Working tree is dirty before running opencode; refusing to let the agent commit tooling artifacts."; exit 1; }

      - name: Configure OpenCode MCP servers
        run: |
          cat > opencode.json << 'EOF'
          {
            "mcp": {
              "serena": {
                "type": "local",
                "command": ["serena", "start-mcp-server", "--context=ide"],
                "enabled": true
              },
              "avalonia-docs": {
                "type": "remote",
                "url": "https://docs-mcp.avaloniaui.net/mcp",
                "enabled": true
              }
            }
          }
          EOF

      - name: Run OpenCode
        uses: anomalyco/opencode/github@v2.0.5
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        with:
          model: opencode/big-pickle
          share: true
          agent: build

